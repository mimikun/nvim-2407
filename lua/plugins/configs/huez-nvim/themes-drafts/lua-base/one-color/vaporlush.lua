-- NOTE: unsupported transparent
---@type LazySpec
local spec = {
    "adamkali/vaporlush",
    --lazy = false,
    dependencies = { "rktjmp/lush.nvim" },
    opts = {},
    --cond = false,
    enabled = false,
}

return spec
